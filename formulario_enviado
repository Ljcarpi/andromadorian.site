<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Gracias — Andromadorian</title>
  <meta name="description" content="Gracias por contactarnos. Tu formulario se envió correctamente.">

  <link rel="icon" type="image/png" href="/assets/favicon.png">
  <link rel="apple-touch-icon" href="/assets/favicon.png">

  <style>
    :root{
      /* Dark theme (default) */
      --bg:
        radial-gradient(1000px 700px at 50% 22%, rgba(124,58,237,.28), transparent 60%),
        radial-gradient(1100px 800px at 50% 78%, rgba(6,182,212,.22), transparent 62%),
        radial-gradient(1400px 900px at 10% 10%, #0e1630 0%, #0b1226 35%, transparent 60%),
        radial-gradient(1200px 800px at 90% 90%, #152645 0%, #0f1a30 40%, #080c14 80%),
        #080c14;

      --card:#0f1830;
      --muted:#a9b4c7;
      --text:#e9eef6;
      --brand:#7c3aed;
      --brand-2:#06b6d4;
      --shadow:0 12px 34px rgba(0,0,0,.45);
      --radius:16px;

      --border: rgba(255,255,255,.12);
      --border-soft: rgba(255,255,255,.10);
      --surface-grad-dark: linear-gradient(180deg,rgba(255,255,255,.05),rgba(255,255,255,.02));

      --input-bg: rgba(255,255,255,.05);
      --input-border: rgba(255,255,255,.18);
      --hdr-bg: rgba(8,12,20,.78);
      --hdr-border: 1px solid rgba(255,255,255,.08);
      --ghost-border: 1px solid rgba(255,255,255,.16);
      --sep: rgba(255,255,255,.14);
      --link-underline: rgba(255,255,255,.7);
    }

    /* Light theme */
    html.theme-light{
      --bg:
        radial-gradient(1000px 700px at 50% 18%, rgba(124,58,237,.16), transparent 60%),
        radial-gradient(1100px 800px at 50% 82%, rgba(6,182,212,.14), transparent 62%),
        #ffffff;

      --card:#ffffff;
      --muted:#49576d;
      --text:#0b1226;
      --brand:#7c3aed;
      --brand-2:#06b6d4;

      --border: rgba(0,0,0,.08);
      --border-soft: rgba(0,0,0,.06);
      --surface-grad-dark: linear-gradient(180deg,rgba(0,0,0,.03),rgba(0,0,0,.015));

      --input-bg: rgba(0,0,0,.035);
      --input-border: rgba(0,0,0,.12);
      --hdr-bg: rgba(255,255,255,.85);
      --hdr-border: 1px solid rgba(0,0,0,.08);
      --ghost-border: 1px solid rgba(0,0,0,.16);
      --sep: rgba(0,0,0,.18);
      --link-underline: rgba(0,0,0,.7);
    }

    *{box-sizing:border-box}
    html,body{margin:0;background:var(--bg);color:var(--text);
      font:16px/1.6 system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Cantarell,Noto Sans,Helvetica,Arial,"Apple Color Emoji","Segoe UI Emoji"}
    a{color:inherit;text-decoration:none}
    img{max-width:100%;display:block}

    .container{width:min(1100px, 92vw);margin-inline:auto}
    header{position:sticky;top:0;background:var(--hdr-bg);backdrop-filter:saturate(200%) blur(10px);border-bottom:var(--hdr-border);z-index:10}
    .nav{display:flex;align-items:center;justify-content:space-between;padding:14px 0}
    .brand{display:flex;gap:.6rem;align-items:center;font-weight:700}
    .brand img{width:28px;height:28px}
    .actions{display:flex;align-items:center;gap:1rem}
    .links{display:flex;gap:1rem;align-items:center}
    .links a{opacity:.9}
    .links a:hover{opacity:1}
    .nav a.btn{padding:.55rem .95rem;border-radius:999px;background:linear-gradient(135deg,var(--brand),var(--brand-2));color:#041015;font-weight:800;box-shadow:0 8px 20px rgba(6,182,212,.25)}

    /* Toolbar: language + theme */
    .toolbar{display:flex;align-items:center}
    .toolbar .sep{width:1px;height:22px;background:var(--sep);margin:0 14px}

    .lang-switch{display:flex;border-radius:999px;overflow:hidden;border:1px solid var(--border)}
    .lang-switch button{
      display:flex;gap:.4rem;align-items:center;
      padding:.35rem .6rem;border:0;background:transparent;color:var(--text);cursor:pointer;
      font-weight:700
    }
    .lang-switch button:hover{filter:saturate(120%)}
    .lang-switch button.active{background:linear-gradient(135deg,var(--brand),var(--brand-2));color:#041015}
    .flag{font-size:1rem;line-height:1}

    .theme-switch{
      display:flex;align-items:center;border-radius:14px;overflow:hidden;
      border:1px solid var(--border); background:var(--surface-grad-dark);
      box-shadow:var(--shadow); margin-left:18px;
    }
    .theme-switch button{
      display:flex;align-items:center;gap:.5rem;
      padding:.45rem .75rem;border:0;background:transparent;color:var(--text);
      cursor:pointer;font-weight:800;text-transform:none;letter-spacing:.1px
    }
    .theme-switch .divider{width:1px;height:20px;background:var(--sep);}
    .theme-switch button:hover{filter:saturate(120%)}
    .theme-switch button.active{
      background:linear-gradient(135deg,var(--brand),var(--brand-2));
      color:#041015;
    }

    /* Hero-like Thank You */
    main{min-height:60vh}
    .ty-hero{position:relative;isolation:isolate}
    .ty-hero::before{
      content:"";position:absolute;inset:-10% -10% 0;z-index:-1;
      background:
        radial-gradient(1200px 650px at 50% 10%, rgba(124,58,237,.35), transparent 60%),
        radial-gradient(900px 520px at 50% 60%, rgba(6,182,212,.28), transparent 58%),
        linear-gradient(180deg, rgba(17,24,39,.5), transparent 60%);
      filter:saturate(130%) contrast(105%);
    }
    html.theme-light .ty-hero::before{
      background:
        radial-gradient(1200px 650px at 50% 10%, rgba(124,58,237,.22), transparent 60%),
        radial-gradient(900px 520px at 50% 60%, rgba(6,182,212,.18), transparent 58%),
        linear-gradient(180deg, rgba(255,255,255,.6), transparent 60%);
    }
    .ty-wrap{padding:80px 0 46px;display:grid;gap:22px;justify-items:center;text-align:center}
    .ty-icon{
      width:84px;height:84px;border-radius:20px;
      display:grid;place-items:center;
      background:linear-gradient(135deg,var(--brand),var(--brand-2));
      box-shadow:0 14px 34px rgba(124,58,237,.22), 0 14px 34px rgba(6,182,212,.18);
      color:#041015;font-size:44px;font-weight:900;
    }
    .ty-wrap h1{font-size:clamp(26px, 2.6vw + 16px, 42px);line-height:1.08;margin:0}
    .ty-wrap p{max-width:780px;color:var(--muted);margin:0}
    .ty-cta{display:flex;gap:12px;flex-wrap:wrap;justify-content:center;margin-top:8px}
    .ty-cta a{display:inline-flex;align-items:center;gap:.5rem;padding:.85rem 1.15rem;border-radius:14px;box-shadow:var(--shadow)}
    .ty-cta .primary{background:linear-gradient(135deg,var(--brand),var(--brand-2));color:#041015;font-weight:900}
    .ty-cta .ghost{border:var(--ghost-border);color:var(--text);opacity:.95}

    /* Footer */
    footer{padding:30px 0 50px;color:var(--muted);border-top:var(--hdr-border)}
    .foot{display:flex;flex-wrap:wrap;gap:12px;align-items:center;justify-content:space-between}
    .small{font-size:.9rem}
    .legal{margin-top:8px;text-align:center}
    .legal a{text-decoration:underline;text-decoration-color:var(--link-underline)}

    /* Floating actions (opcional, útiles aquí también) */
    .float-cta{position:fixed;right:18px;bottom:18px;z-index:20}
    .float-cta a{display:inline-flex;align-items:center;gap:.5rem;padding:.85rem 1.05rem;border-radius:999px;background:linear-gradient(135deg,var(--brand),var(--brand-2));color:#041015;font-weight:900;box-shadow:0 12px 28px rgba(6,182,212,.22)}

    .whatsapp-float {
      position: fixed; width: 60px; height: 60px;
      bottom: 90px; right: 18px; background-color: #25d366;
      color: #FFF; border-radius: 50px; text-align: center;
      box-shadow: 2px 2px 6px rgba(0,0,0,0.4); z-index: 100;
      display: flex; align-items: center; justify-content: center;
      transition: background-color .2s ease;
    }
    .whatsapp-float:hover { background-color: #128C7E; }
    .whatsapp-float svg { width: 32px; height: 32px; fill: white; }
  </style>
</head>
<body>
  <!-- Header con nav + toggles -->
  <header>
    <div class="container nav">
      <a class="brand" href="/" aria-label="Inicio" data-i18n-attr="aria-label:aria_brand">
        <img src="/assets/icon.png" alt="Andromadorian" />
        <span>Andromadorian</span>
      </a>

      <div class="actions">
        <nav class="links">
          <a href="/#ejemplos" data-i18n="nav_examples">Ejemplos</a>
          <a href="/#contacto" data-i18n="nav_contact">Contacto</a>
          <a class="btn" href="/#paquetes" data-i18n="nav_packages">Ver paquetes</a>
        </nav>

        <div class="toolbar" aria-label="Controles">
          <div class="lang-switch" role="group" aria-label="Idioma / Language">
            <button type="button" class="active" data-lang="es" aria-pressed="true">
              <span class="flag" aria-hidden="true">🇪🇸</span> <span>ES</span>
            </button>
            <div class="sep" aria-hidden="true" style="height:18px;margin:0"></div>
            <button type="button" data-lang="en" aria-pressed="false">
              <span class="flag" aria-hidden="true">🇺🇸</span> <span>EN</span>
            </button>
          </div>

          <div class="sep" aria-hidden="true"></div>

          <div class="theme-switch" role="group" aria-label="Tema">
            <button type="button" data-theme="dark" aria-pressed="true" class="active">
              <span class="ico" aria-hidden="true">☾</span>
              <span data-i18n="theme_dark">Tema oscuro</span>
            </button>
            <div class="divider" aria-hidden="true"></div>
            <button type="button" data-theme="light" aria-pressed="false">
              <span class="ico" aria-hidden="true">☀</span>
              <span data-i18n="theme_light">Tema claro</span>
            </button>
          </div>
        </div>
      </div>
    </div>
  </header>

  <!-- Contenido de agradecimiento -->
  <main>
    <section class="ty-hero">
      <div class="container ty-wrap">
        <div class="ty-icon" aria-hidden="true">✓</div>
        <h1 data-i18n="ty_title">¡Gracias por elegirnos!</h1>
        <p data-i18n="ty_text">
          Tu formulario se envió correctamente. En breve me pondré en contacto para coordinar los próximos pasos.
          Si querés, podés explorar los paquetes o volver al inicio.
        </p>

        <div class="ty-cta">
          <a class="primary" href="/#paquetes" data-i18n="ty_cta_packages">Ver paquetes</a>
          <a class="ghost" href="/" data-i18n="ty_cta_home">Volver al inicio</a>
        </div>

        <p class="small" style="margin-top:10px;color:var(--muted)" data-i18n="ty_autoredirect">
          Te redirigiremos al inicio automáticamente en unos segundos…
        </p>
      </div>
    </section>
  </main>

  <!-- Footer con leyendas legales -->
  <footer>
    <div class="container">
      <div class="foot">
        <span class="brand small" style="opacity:.9"><img src="/assets/icon.png" alt="Andromadorian"/> Andromadorian</span>
        <span class="small">© <span id="year"></span> — <span data-i18n="footer_tag">Sitios web profesionales</span></span>
      </div>
      <p class="legal small">
        <span data-i18n="legal_prefix">Al usar este sitio aceptás nuestros</span>
        <a href="/tyc.html" data-i18n="legal_terms">Términos y condiciones</a>
        <span> </span>
        <span data-i18n="legal_and">y</span>
        <a href="/priv.html" data-i18n="legal_priv">Privacidad</a>.
      </p>
    </div>
  </footer>

  <!-- Acciones flotantes (opcional) -->
  <div class="float-cta"><a href="/#paquetes" data-i18n="float_cta">Ver paquetes</a></div>

  <a href="https://wa.me/5491162857653" class="whatsapp-float" target="_blank" rel="noopener noreferrer" aria-label="Contactar por WhatsApp">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 448 512">
      <path d="M380.9 97.1C339 55.1 283.2 32 223.9 32c-122.4 0-222 99.6-222 222 0 39.1 10.2 77.3 29.6 111L0 480l117.7-30.9c32.4 17.7 68.9 27 106.1 27h.1c122.3 0 221.9-99.6 221.9-222 0-59.3-25.2-115-67.1-157zm-157 341.6c-33.8 0-67.6-9.5-97.8-26.7l-7-4.1-72.5 19.1 19.4-70.5-4.5-7.3C51.9 358.3 48 322.9 48 288c0-97.2 79.2-176 176-176s176 78.8 176 176-79.2 176-176 176zm94.4-118.9l-20.2-10.1c-12.2-6.1-20.3-5.1-24.9 5.1-4.6 10.2-16.4 20.3-21 24.9-4.6 4.6-9.2 5.1-16.4 2.5-7.2-2.5-30.7-11.2-58.3-35.4-21.5-18.9-35.4-42.5-38.9-49.7-3.5-7.2-1.5-11.2 2.5-15.3 3.5-3.5 7.7-9.2 10.2-12.2 2.5-3.1 5.1-6.1 7.2-10.2 2.5-4.6.5-9.2-2-11.7l-15.3-36.8c-2.5-6.1-5.1-7.2-9.2-7.2-3.1 0-7.2 0-11.2 0-4.1 0-11.2 1.5-16.4 7.2-5.1 5.1-20.3 19.4-20.3 46.6 0 27.2 20.8 54.3 23.9 58.9 3.1 4.6 40.5 64.9 98.4 86.8 14.2 5.1 27.5 8.2 37.8 10.7 10.2 2.5 19.4 1.5 26.7-1.5 7.7-3.5 20.3-8.7 23.4-16.4 3.1-7.7 3.1-14.2 2-16.4z"/>
    </svg>
  </a>

  <script>
    // Año footer
    document.getElementById('year').textContent = new Date().getFullYear();

    // Diccionario i18n (extiende el de la landing con claves de "gracias")
    const T = {
      es: {
        meta_title: "Gracias — Andromadorian",
        meta_desc: "Gracias por contactarnos. Tu formulario se envió correctamente.",
        aria_brand: "Inicio",
        nav_examples: "Ejemplos",
        nav_contact: "Contacto",
        nav_packages: "Ver paquetes",
        footer_tag: "Sitios web profesionales",
        legal_prefix: "Al usar este sitio aceptás nuestros",
        legal_terms: "Términos y condiciones",
        legal_and: "y",
        legal_priv: "Privacidad",
        float_cta: "Ver paquetes",
        theme_dark: "Tema oscuro",
        theme_light: "Tema claro",

        /* Thank you content */
        ty_title: "¡Gracias por elegirnos!",
        ty_text: "Tu formulario se envió correctamente. En breve me pondré en contacto para coordinar los próximos pasos. Si querés, podés explorar los paquetes o volver al inicio.",
        ty_cta_packages: "Ver paquetes",
        ty_cta_home: "Volver al inicio",
        ty_autoredirect: "Te redirigiremos al inicio automáticamente en unos segundos…"
      },
      en: {
        meta_title: "Thanks — Andromadorian",
        meta_desc: "Thanks for reaching out. Your form was submitted successfully.",
        aria_brand: "Home",
        nav_examples: "Examples",
        nav_contact: "Contact",
        nav_packages: "See packages",
        footer_tag: "Professional websites",
        legal_prefix: "By using this site you accept our",
        legal_terms: "Terms & Conditions",
        legal_and: "and",
        legal_priv: "Privacy",
        float_cta: "See packages",
        theme_dark: "Dark theme",
        theme_light: "Light theme",

        /* Thank you content */
        ty_title: "Thanks for choosing us!",
        ty_text: "Your form was sent successfully. I’ll get back to you shortly to coordinate next steps. Meanwhile, you can browse packages or return to the homepage.",
        ty_cta_packages: "See packages",
        ty_cta_home: "Back to home",
        ty_autoredirect: "We’ll redirect you to the homepage in a few seconds…"
      }
    };

    const $ = (sel, root=document) => root.querySelector(sel);
    const $$ = (sel, root=document) => Array.from(root.querySelectorAll(sel));

    function applyI18n(lang){
      const dict = T[lang] || T.es;

      // text nodes
      $$("[data-i18n]").forEach(el=>{
        const key = el.getAttribute("data-i18n");
        if (key && dict[key] !== undefined){
          el.innerHTML = dict[key];
        }
      });

      // attribute mappings
      $$("[data-i18n-attr]").forEach(el=>{
        const map = el.getAttribute("data-i18n-attr").split(";").map(s=>s.trim()).filter(Boolean);
        map.forEach(pair=>{
          const [attr, k] = pair.split(":").map(s=>s.trim());
          if (attr && k && dict[k] !== undefined){
            el.setAttribute(attr, dict[k]);
          }
        });
      });

      // document title & meta
      document.title = dict.meta_title;
      const meta = document.querySelector('meta[name="description"]');
      if (meta) meta.setAttribute("content", dict.meta_desc);

      // update html lang
      document.documentElement.setAttribute("lang", lang);

      // aria on brand link
      const brand = $(".brand");
      if (brand) brand.setAttribute("aria-label", dict.aria_brand);

      // update active state on lang buttons
      $$('.lang-switch button').forEach(btn=>{
        const isActive = btn.getAttribute('data-lang') === lang;
        btn.classList.toggle('active', isActive);
        btn.setAttribute('aria-pressed', String(isActive));
      });

      localStorage.setItem("lang", lang);
    }

    function applyTheme(theme){
      const isLight = theme === 'light';
      document.documentElement.classList.toggle('theme-light', isLight);

      // Actualiza estado del control segmentado
      $$('.theme-switch button').forEach(btn=>{
        const thisIsActive = btn.getAttribute('data-theme') === (isLight ? 'light' : 'dark');
        btn.classList.toggle('active', thisIsActive);
        btn.setAttribute('aria-pressed', String(thisIsActive));
      });

      localStorage.setItem('theme', isLight ? 'light' : 'dark');
    }

    (function init(){
      const lang = localStorage.getItem('lang') || 'es';
      applyI18n(lang);

      const theme = localStorage.getItem('theme') || 'dark';
      applyTheme(theme);

      // listeners idioma
      $$('.lang-switch button').forEach(btn=>{
        btn.addEventListener('click', ()=>applyI18n(btn.getAttribute('data-lang')));
      });

      // listeners tema
      $$('.theme-switch button').forEach(btn=>{
        btn.addEventListener('click', ()=>{
          const next = btn.getAttribute('data-theme');
          applyTheme(next);
        });
      });

      // Auto-redirect opcional (7s). Comenta este bloque si no lo querés.
      setTimeout(()=>{ window.location.href = "/"; }, 7000);
    })();
  </script>
</body>
</html>
